<% @shipments.each do |shipment| %>
	<h2><%= link_to shipment.date, shipment_path(shipment) %></h2>
	<%= link_to "Delete", shipments_path(shipment), method: :delete, data: {confirm: "Are you sure you want to delete this student?"} %>
<% end %>